// Generator when called 'yo ankit:router'
var generators = require('yeoman-generator'),
    say = require('yosay');

// Extend NamedBase if you require a name argument for generator
// For example in 'yo name:router foo' , foo will be assigned to this.name
module.exports = generators.NamedBase.extend({
  /*
    Helper/Private Methods can be created in three ways :-

    1) Prefix method name with a underscore 
       _helperMethod: function() { console.log('won\'t be called automatically'); }

    2) Use Instance Method
       this.helperMethod = function() { console.log('won\'t be called automatically'); }

    3) Extend a parent generator
       var MyBase = generators.Base.extend({
         helper: function () { console.log('won\'t be called automatically'); }
       });

       module.exports = MyBase.extend({
         exec: function () { this.helper(); }
       });
  */
});